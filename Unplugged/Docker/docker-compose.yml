services:
  server:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ${APNS_PRIVATE_KEY_FILE_HOST}:/run/secrets/apns_key.p8:ro
    restart: unless-stopped
