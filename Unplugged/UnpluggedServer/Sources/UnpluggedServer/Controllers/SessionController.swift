//
//  SessionController.swift
//  UnpluggedServer.Controllers
//
//  Created by Sebastian Gonzalez on 3/12/26.
//

// TODO: Implement SessionController (RouteCollection) — POST /sessions (create room), GET /sessions (list open/joinable rooms), GET /sessions/:id (get session details + participants), POST /sessions/:id/join, POST /sessions/:id/start (host only, transition to countdown), POST /sessions/:id/end (host only), GET /sessions/history (user's past sessions)
